[build]
  functions = "netlify/functions"
  publish = "."

[functions]
  node_bundler = "esbuild"
  # Include the Word template in function bundles
  included_files = ["netlify/functions/*.docx"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
